var num = Math.floor(Math.random() * 4) + 1;

$("#card1").wScratchPad({
  size: 100, // The size of the brush/scratch.
  bg: `Images/1.jpeg`, // Background (image path or hex color).
  fg: `Images/front.jpg`, // Foreground (image path or hex color).
  cursor: "pointer", // Set cursor.
});

$("#card2").wScratchPad({
  size: 100, // The size of the brush/scratch.
  bg: `Images/2.jpeg`, // Background (image path or hex color).
  fg: `Images/front.jpg`, // Foreground (image path or hex color).
  cursor: "pointer", // Set cursor.
});
$("#card3").wScratchPad({
  size: 100, // The size of the brush/scratch.
  bg: `Images/4.jpeg`, // Background (image path or hex color).
  fg: `Images/front.jpg`, // Foreground (image path or hex color).
  cursor: "pointer", // Set cursor.
});

$("#card4").wScratchPad({
  size: 100, // The size of the brush/scratch.
  bg: `Images/5.jpeg`, // Background (image path or hex color).
  fg: `Images/front.jpg`, // Foreground (image path or hex color).
  cursor: "pointer", // Set cursor.
});
$("#card5").wScratchPad({
  size: 100, // The size of the brush/scratch.
  bg: `Images/6.jpeg`, // Background (image path or hex color).
  fg: `Images/front.jpg`, // Foreground (image path or hex color).
  cursor: "pointer", // Set cursor.
});

$("#card6").wScratchPad({
  size: 100, // The size of the brush/scratch.
  bg: `Images/7.jpeg`, // Background (image path or hex color).
  fg: `Images/front.jpg`, // Foreground (image path or hex color).
  cursor: "pointer", // Set cursor.
});
$("#card7").wScratchPad({
  size: 100, // The size of the brush/scratch.
  bg: `Images/8.jpeg`, // Background (image path or hex color).
  fg: `Images/front.jpg`, // Foreground (image path or hex color).
  cursor: "pointer", // Set cursor.
});

$("#card8").wScratchPad({
  size: 100, // The size of the brush/scratch.
  bg: `Images/9.jpeg`, // Background (image path or hex color).
  fg: `Images/front.jpg`, // Foreground (image path or hex color).
  cursor: "pointer", // Set cursor.
});
